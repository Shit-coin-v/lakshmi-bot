# 2026-02-17

## Picker Bot — Feature Complete & Pushed to `dev`

### What was built
- **Picker bot** (`bots/picker_bot/`, 788 lines, 12 files) — Telegram bot for order pickers
- **Backend**: New `accepted` status, `assembled_by` field, 5 bot_api endpoints, `PickerNotificationMessage` model, Celery task for picker notifications
- **Flutter**: `accepted` status (cyan), `fulfillmentType` in models, split timeline (delivery 7 steps / pickup 5), pickup-specific UX
- **Infra**: `Dockerfile.picker`, docker-compose service, `.env.example`

### Order status flow
- Full: `new → accepted → assembly → ready → delivery → arrived → completed` (+ `canceled`)
- Picker handles: `new → accepted → assembly → ready` (all), plus `ready → completed` (pickup only)
- Courier handles: `ready → delivery → arrived → completed` (delivery only)

### Key decisions
- 3-step picker flow (not 2) — Василий requested intermediate "Принять"
- `assembled_by` tracks which picker took order
- "↩️ Вернуть заказ" resets to `new` + clears `assembled_by`
- No assembly rate in `/completed` — count only
- Removed dev stub in `send_order_to_onec` that auto-advanced `new→ready`

### Commits
- `b2e1faa` — main picker bot feature
- `88d948f` — dev stub fix
- CI passed (run #73)

### Status
- Waiting for Василий to deploy and test locally
- Deploy cmd: `docker compose up -d --build app celery_worker celery_beat picker_bot` + migrations
- Needs `PICKER_BOT_TOKEN` and `PICKER_ALLOWED_TG_IDS` in `.env`

### Tech debt noted
- `chat_cleanup.py` and `retry.py` duplicated between courier_bot and picker_bot → future refactor to `shared/bot_utils/`
