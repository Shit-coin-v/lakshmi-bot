server {
    listen 80;
    server_name 158.160.183.136;

    # Динамическое резолвинг-имён контейнеров Docker (чтобы не ловить 502 после смены IP) 🔁
    resolver 127.0.0.11 ipv6=off valid=10s;
    set $backend  http://app:8000;
    set $metabase http://metabase:3000;

    # Разумные лимиты для API/загрузок
    client_max_body_size 20m;

    # Быстрый health самого Nginx
    location = /nginx/health {
        return 200 'ok';
        add_header Content-Type text/plain;
    }

    # Статика Django
    location /static/ {
        alias /app/backend/collected_static/;
        expires 30d;
        access_log off;
    }

    # Медиа Django
    location /media/ {
        alias /app/media/;
        expires 30d;
        access_log off;
    }

    # Прокси в Django (Gunicorn в контейнере app) — сюда попадают /onec/* и всё остальное
    location / {
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 3s;
        proxy_send_timeout    60s;
        proxy_read_timeout    60s;

        proxy_pass $backend;
    }

    # Аккуратный редирект на /metabase/ со слешем
    location = /metabase {
        return 301 /metabase/;
    }

    # Прокси на Metabase с корректными заголовками префикса
    location /metabase/ {
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;
        proxy_set_header X-Forwarded-Prefix /metabase/;

        proxy_connect_timeout 3s;
        proxy_send_timeout    60s;
        proxy_read_timeout    60s;

        proxy_pass $metabase;
    }
}

