# Redis configuration for lakshmi-bot
# Password is passed via --requirepass in docker-entrypoint.sh (not visible in docker ps)

# Persistence (AOF)
appendonly yes
appendfsync everysec

# Memory
maxmemory 256mb
maxmemory-policy allkeys-lru

# Network
bind 0.0.0.0
protected-mode yes
